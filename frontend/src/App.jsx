import { useState, useEffect, useRef } from 'react'
import * as THREE from 'three'
import './App.css'

const API = import.meta.env.VITE_API_URL || 'http://localhost:3001'

// TEMP - full content will be restored in next step if this succeeds
export default function App() {
  return <div style={{color:'#fff',padding:40,fontFamily:'system-ui'}}>XhumAI — restoring full combo lander...</div>
}
